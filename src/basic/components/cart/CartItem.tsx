import { type FC } from 'react';
import Layout from 'refactor/layout/Default';
import styles from './styles.module.scss';

interface IProps {
  
}

const CartItem: FC<IProps> = ({  }) => {
  return (
    <Layout>
      <div className={styles.layout}>
        
      </div>
    </Layout>
  );
};

export default CartItem;