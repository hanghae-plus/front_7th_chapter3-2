# 📚 과제 개념 문서

이 폴더는 **Chapter 3-2 과제**를 수행하기 위해 알아야 할 핵심 개념들을 정리한 문서입니다.

---

## 🎯 과제 목표

- React의 Hook 이해하기
- 함수형 프로그래밍에 대한 이해
- 액션과 순수함수의 분리
- Props Drilling 해결

---

## 📖 문서 목록

| 번호 | 문서                                                             | 설명                                          |
| ---- | ---------------------------------------------------------------- | --------------------------------------------- |
| 00   | [선언형과 함수형 프로그래밍](./00-선언형과-함수형-프로그래밍.md) | 명령형 vs 선언형, 함수형 프로그래밍 핵심 원칙 |
| 01   | [함수형 프로그래밍 기초](./01-함수형-프로그래밍-기초.md)         | 액션/계산/데이터 분리, 순수 함수 작성법       |
| 02   | [엔티티와 계층 구조](./02-엔티티와-계층-구조.md)                 | 엔티티 개념, models → hooks → components 계층 |
| 03   | [React Hook 패턴](./03-React-Hook-패턴.md)                       | useCart, useProducts 등 커스텀 훅 구현        |
| 04   | [Compound Components 패턴](./04-Compound-Components-패턴.md)     | 선언적 컴포넌트 API 설계                      |
| 05   | [전역 상태 관리](./05-전역상태관리-Context-vs-Jotai.md)          | Context vs Jotai 비교 (심화과제)              |
| 06   | [과제 구현 가이드](./06-과제-구현-가이드.md)                     | 단계별 구현 순서, 체크리스트                  |
| 07   | [리팩토링 설계 철학](./07-리팩토링-설계-철학.md)                 | 관심사 분리, 코드 배치 기준, 계층별 역할      |
| 08   | [데이터/계산/액션 분류](./08-데이터-계산-액션-분류.md)           | 함수형 3분류, 파일별 분류, 실전 적용 가이드   |
| 09   | [리팩토링 실전 기록](./09-리팩토링-실전-기록.md)                 | 훅별 문제점, 해결 과정, 이론적 근거 상세 기록 |

---

## 📋 읽는 순서

### 기본과제 준비

```
00 → 01 → 02 → 03 → 06
```

1. **00-선언형과-함수형-프로그래밍**: 선언형/함수형 개념 이해
2. **01-함수형-프로그래밍-기초**: 액션/계산 분리, 순수 함수 작성법
3. **02-엔티티와-계층-구조**: 코드를 어떻게 나눌지 이해
4. **03-React-Hook-패턴**: 훅 구현 방법 학습
5. **06-과제-구현-가이드**: 실제 구현 시작

### 심화과제 준비

```
04 → 05 → 06
```

1. **04-Compound-Components-패턴**: (선택) 고급 컴포넌트 패턴
2. **05-전역상태관리**: Context 또는 Jotai 선택
3. **06-과제-구현-가이드**: 심화과제 구현

> ⚠️ **주의**: 기본과제에서는 Context/Jotai를 사용하지 않습니다. 05번 문서는 심화과제용입니다.

---

## 🗂️ 목표 폴더 구조

```
src/basic/
├── models/           # 🧮 순수 함수 (계산)
│   ├── cart.ts
│   ├── product.ts
│   └── coupon.ts
│
├── hooks/            # ⚡ 커스텀 훅 (액션)
│   ├── useCart.ts
│   ├── useProducts.ts
│   ├── useCoupons.ts
│   └── useNotification.ts
│
├── utils/            # 🔧 유틸리티
│   ├── formatters.ts
│   └── hooks/
│       └── useDebounce.ts
│
├── components/       # 🎨 UI 컴포넌트
│   ├── Cart/
│   ├── Product/
│   └── Admin/
│
└── App.tsx
```

---

## ✅ 핵심 체크리스트

### 기본과제

- [ ] 순수 함수가 `models/`에 분리됨
- [ ] 커스텀 훅이 `hooks/`에 분리됨
- [ ] 포맷팅 함수가 `utils/`에 분리됨
- [ ] 계층 간 의존성 방향이 올바름 (models ← hooks ← components)

### 심화과제

- [ ] 전역 상태 관리 구현 (Context 또는 Jotai)
- [ ] Props Drilling 제거
- [ ] 도메인 props는 유지

---

## 📝 핵심 개념 요약

### 1. 액션 vs 계산

```typescript
// 계산 (순수 함수) - models/
function calculateTotal(cart: CartItem[]): number {
  return cart.reduce((sum, item) => sum + item.price * item.quantity, 0);
}

// 액션 (부작용 있음) - hooks/
function addToCart(product: Product) {
  setCart((prev) => [...prev, product]); // 상태 변경
  addNotification("담았습니다"); // 부작용
}
```

### 2. 엔티티 vs UI 상태

```typescript
// 엔티티 상태 (비즈니스 데이터)
const [cart, setCart] = useState<CartItem[]>([]);
const [products, setProducts] = useState<Product[]>([]);

// UI 상태 (화면 제어)
const [isAdmin, setIsAdmin] = useState(false);
const [showForm, setShowForm] = useState(false);
```

### 3. Props 제거 기준

```tsx
// ❌ 제거: 전역 상태/액션
onAddToCart = { addToCart };
cart = { cart };

// ✅ 유지: 도메인 데이터
product = { product };
item = { cartItem };
```
